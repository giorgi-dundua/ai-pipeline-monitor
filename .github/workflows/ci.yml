name: CI

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "CI is configured. Add ruff/pytest steps here later."
