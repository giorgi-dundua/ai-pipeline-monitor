name: Deploy

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Deployment is configured. Add Docker/Cloud steps here later."
